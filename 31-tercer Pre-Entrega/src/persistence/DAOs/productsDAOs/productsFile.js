import fs from 'fs';
import { __dirname } from '../../../utils.js';

const path = __dirname + '';

export default class ProductsFileManager {
  async getAllProducts(limit, page) {}

  async getProductById(id) {}

  async addProduct(prod) {}

  async deleteProduct(id) {}
}
