<h2 style='color:royalblue'>¡Hola {{user.name}}!  
</h2>
<br />
<br />

<div class='profile__card'>

  <div class='profile__card--img'>
    <img src={{user.thumbnail}} alt={{user.name}} />
  </div>

  <div class='profile__card--info'>
    <p>name: <span>{{user.name}}</span></p>
    <p>surname: <span>{{user.surname}}</span></p>
    <p>email: <span>{{user.email}}</span></p>
    <p>rol: <span>{{user.rol}}</span></p>
    <p>cart: <span>{{user.cart}}</span></p>
  </div>

  <div class="profile__card--buttons">
    <button onclick="location.href='/cart/{{user.cart}}'">My CART</button>
  <button onclick="location.href='/user/logout'">LOGOUT</button>

  </div>

  
</div>

{{!-- {{#if user}}
      {{this.name}}
      {{else}}
      no name
    {{/if}}

<div class='profile__card'>
  <div class='profile__card--info'>
    {{#each user}}
      {{#if name}}
        <p>name: <span>{{this.name}}</span></p>
      {{/if}}
      {{#if last_name}}
        <p>surname: <span>{{this.last_name}}</span></p>
      {{/if}}
      {{#if email}}
        <p>email: <span>{{this.email}}</span></p>
      {{/if}}
      {{#if rol}}
        <p>rol: <span>{{this.rol}}</span></p>
      {{/if}}
    {{/each}}
    </div>

<button onclick="location.href='/user/logout'">LOGOUT</button>

</div> --}}
<br />
<br />